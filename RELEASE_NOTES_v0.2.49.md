# CRM CLI v0.2.49

发布日期：2026-06-08

## 新增功能

- 新增租户自注册命令组 `crm auth register-sms` 与 `crm auth register-tenant`。
- 支持先发送手机号验证码，再用验证码完成租户注册的两步流程。
- 注册命令支持邮箱可空输入，适配没有邮箱的注册场景。
- 注册成功后输出租户 ID、套餐、配额、有效期和创建时间，便于手动核对。

## 相关变化

- `crm auth` 命令总览已加入租户注册入口。
- 发布流程中同步包含最新的 `skills/` 内容。
